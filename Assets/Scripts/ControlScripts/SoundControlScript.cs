using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundControlScript : MonoBehaviour {
	void Start () { Debug.LogError(this.name + " has deprecated sound control script. Remove it!"); }
}
